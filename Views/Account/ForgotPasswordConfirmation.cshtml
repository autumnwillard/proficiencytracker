<h2>Password Reset Requested</h2>
<p>If an account with the provided email exists, we've sent a password reset link.</p>

@if (ViewBag.ResetLink != null)
{
    <p>For local testing, here's your reset link:</p>
    <a href="@ViewBag.ResetLink">Reset Password</a>
}
